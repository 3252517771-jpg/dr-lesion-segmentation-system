# 2026-06-14 阶段记录

## M1 + M2 完成

本阶段完成后端 Flask 脚手架、数据库模型、迁移与基础 CRUD。

### 已完成内容

- 初始化 git 仓库。
- 新增 Flask app factory：`backend/app.py`。
- 新增后端配置、扩展与统一错误处理：
  - `backend/config.py`
  - `backend/extensions.py`
  - `backend/errors.py`
- 新增健康检查接口：`GET /api/health`。
- 新增 SQLAlchemy ORM 模型：
  - `Patient`
  - `Diagnosis`
  - SQLite 兼容 `JSONField`
- 新增 Alembic 迁移：
  - `backend/database/migrations/versions/30496e383b13_create_patients_and_diagnoses.py`
- 新增 Patient CRUD：
  - `GET /api/patients`
  - `POST /api/patients`
  - `GET /api/patients/<id>`
  - `PUT /api/patients/<id>`
  - `DELETE /api/patients/<id>`
- 新增 Diagnosis CRUD：
  - `GET /api/diagnoses`
  - `POST /api/diagnoses`
  - `GET /api/diagnoses/<id>`
  - `PUT /api/diagnoses/<id>`
  - `DELETE /api/diagnoses/<id>`
- 新增后端测试：
  - `backend/tests/test_health.py`
  - `backend/tests/test_patients.py`
  - `backend/tests/test_diagnoses.py`

### 验证

- 使用环境：`A:\python1\envs\cardio-ai\python.exe`
- 已安装后端依赖：`backend/requirements.txt`
- 测试命令：`A:\python1\envs\cardio-ai\python.exe -m pytest backend\tests -q`
- 测试结果：`4 passed`
- 数据库迁移状态：`30496e383b13 (head)`
- 启动 `backend/app.py` 后访问 `/api/health` 返回 `200`，数据库连接和模型文件检测正常。

### 已知状态

- 当前尚未配置 git remote，因此本阶段只能本地提交，不能 push。
- `POST /api/diagnose` 上传推理接口属于 M3/M4，不在本阶段实现。
- ML 推理层、图片服务、统计接口将在后续里程碑继续实现。

## M3 完成

本阶段完成 ML 层：Attention UNet 定义、真实模型加载、推理管线与后处理工具。

### 已完成内容

- 新增 `backend/ml/model.py`
  - 与 Kaggle 训练脚本中的 Attention UNet 结构保持一致。
  - features 固定为 `(32, 64, 128, 256)`。
  - 支持输入尺寸恢复，输出 4 通道：HE/EX/MA/SE。
- 新增 `backend/ml/config.py`
  - 管理模型路径、阈值文件路径、输入尺寸、类别顺序、ImageNet 归一化参数。
  - 读取 `backend/trained_models/recommended_thresholds.json`。
- 新增 `backend/ml/utils.py`
  - 眼底 ROI 构建。
  - 连通域过滤。
  - 多类别 mask 后处理。
  - mask 恢复原图尺寸。
  - 病灶面积占比计算。
  - 病灶连通域数量计算。
  - severity 经验分级。
  - 轮廓叠加图生成。
- 新增 `backend/ml/inference.py`
  - `RealInference.load_model()`
  - `RealInference.preprocess()`
  - `RealInference.predict_masks()`
  - `RealInference.predict()`
- 新增 `backend/tests/test_ml.py`
  - 验证模型输出 shape。
  - 验证阈值文件读取。
  - 验证后处理、面积、数量、轮廓生成。
  - 使用真实 `attention_unet_dr.pth` 做 CPU 小图推理测试。

### 验证

- 使用环境：`A:\python1\envs\cardio-ai\python.exe`
- 单独 ML 测试：`A:\python1\envs\cardio-ai\python.exe -m pytest backend\tests\test_ml.py -q`
- 单独 ML 测试结果：`4 passed`
- 全部后端测试：`A:\python1\envs\cardio-ai\python.exe -m pytest backend\tests -q`
- 全部后端测试结果：`8 passed`
- 健康检查：`GET /api/health` 返回 `200`
- 模型兼容性：
  - state_dict missing keys: `0`
  - state_dict unexpected keys: `0`
  - 参数量：`7,851,872`

### 已知状态

- M3 只实现独立 ML 层，不直接改 API 调用链。
- `backend/services/inference_service.py` 与 `POST /api/diagnose` 属于 M4，将在下一阶段接入。
- 真实模型文件 `backend/trained_models/attention_unet_dr.pth` 继续被 `.gitignore` 排除，不推送到 GitHub。

## M4 + M5 + M6 完成

本阶段完成后端推理服务、前端核心页面，以及上传诊断闭环联调。

### M4 后端

- 新增 `backend/services/image_service.py`
  - 上传图片格式校验。
  - 保存原图到 `backend/uploads/originals`。
  - 安全解析 `/api/images/<path>`。
- 新增 `backend/services/inference_service.py`
  - 支持 `MODEL_BACKEND=auto|real|placeholder`。
  - `auto` 优先真实模型，失败回退 placeholder。
  - `real` 模式失败时返回模型/推理错误。
  - placeholder 模式生成稳定演示诊断数据与轮廓图。
- 新增 `backend/api/diagnose.py`
  - `POST /api/diagnose`
- 新增 `backend/api/images.py`
  - `GET /api/images/<path>`
- 新增 `backend/services/stats_service.py` 和 `backend/api/stats.py`
  - `GET /api/stats/overview`
  - `GET /api/stats/lesions`
  - `GET /api/stats/trend`
- 诊断结果响应补充 `image_url` 与 `contour_url`，方便前端直接展示。

### M5 前端

- 新增 Vite + React 18 + TypeScript + Ant Design 项目骨架。
- 新增 React Router 路由：
  - `/`
  - `/diagnose`
  - `/diagnose/:id`
  - `/patients`
  - `/patients/:id`
- 新增 axios API 层、TanStack Query hooks、基础类型定义。
- 新增核心页面：
  - Dashboard
  - Diagnose
  - DiagnosisDetail
  - Patients
  - PatientDetail
- 新增毛玻璃布局、侧边导航、诊断报告、病灶图例、轮廓图组件。

### M6 联调

- Vite 代理 `/api` 到 Flask `127.0.0.1:5000`。
- 完成 HTTP 联调：
  - 创建患者。
  - 上传眼底图片。
  - 后端真实模型 `mode=real` 推理。
  - 写入诊断记录。
  - 读取诊断详情。
  - 获取轮廓图。
  - 前端详情页可访问。

### 验证

- 后端测试：`A:\python1\envs\cardio-ai\python.exe -m pytest backend\tests -q`
- 后端测试结果：`12 passed`
- 前端构建：`npm run build`
- 前端构建结果：通过。
- 联调结果：
  - frontend index: `200`
  - frontend detail page: `200`
  - backend diagnosis mode: `real`
  - contour image: `200`

### 已知状态

- Three.js 球体病灶分布保留到 M7。
- ECharts 3D Dashboard 深度图表保留到 M8。
- 联调产生的本地上传图、轮廓图、SQLite 数据库和模型权重均被 `.gitignore` 排除。

## M7 完成

本阶段完成诊断详情页 Three.js 3D 病灶分布组件。

### 已完成内容

- 新增依赖：
  - `three`
  - `@react-three/fiber`
  - `@react-three/drei`
  - `playwright` 作为开发验证依赖
- 新增 `frontend/src/components/three/LesionSphere.tsx`
  - 半透明眼球球体。
  - 眼球表面网格。
  - 四类病灶彩色方块标记。
  - OrbitControls 支持拖拽旋转和滚轮缩放。
  - hover tooltip 展示病灶类别、数量、面积占比。
  - 根据诊断记录中的 `lesion_counts` 与 `lesion_areas` 生成稳定球面点位。
- 新增 `frontend/src/components/three/lesion-sphere.css`
  - 3D 容器、图例、底部摘要和 tooltip 样式。
- 更新 `frontend/src/pages/DiagnosisDetail.tsx`
  - 使用 `React.lazy` 懒加载 `LesionSphere`。
  - 仅切换到 `3D 分布` Tab 时渲染 3D Canvas。

### 验证

- 后端测试：`A:\python1\envs\cardio-ai\python.exe -m pytest backend\tests -q`
- 后端测试结果：`12 passed`
- 前端构建：`npm run build`
- 前端构建结果：通过。
- Playwright 浏览器验证：
  - 详情页可打开。
  - `3D 分布` Tab 可切换。
  - Canvas 存在。
  - WebGL context 存在。
  - 截图确认球体、网格、病灶方块、图例、摘要均正常显示。
- 人工浏览器确认：3D 分布在用户浏览器中正常显示。

### 已知状态

- 当前 3D 点位来自诊断统计数据生成，不是像素级真实坐标。
- 如果后续后端返回病灶像素坐标或连通域中心点，`LesionSphere` 可直接改为使用真实位置。
- Dashboard 的 ECharts 3D 图表仍留到 M8。

## M7 Scheme C Upgrade

本次将诊断详情页 Three.js 组件升级为真实眼底纹理球面与真实病灶坐标驱动展示。

### 已完成内容

- 后端新增 `lesion_positions` 诊断字段，并同步数据库模型、API 返回和 Alembic 迁移。
- ML 后处理基于最终 mask 做连通域分析，返回每个病灶的归一化中心点、像素面积、面积占比和 bbox。
- placeholder 模式新增稳定假坐标，保证演示模式和真实模式使用同一前端字段。
- 前端 `LesionSphere` 使用原始眼底图作为球面纹理，原方块标记替换为贴合球面的半透明病灶斑纹。
- 老诊断记录没有 `lesion_positions` 时自动回退到统计量生成点位。

### 验证

- 后端测试：`A:\python1\envs\cardio-ai\python.exe -m pytest backend\tests -q`
- 结果：`12 passed`
- 前端构建：`npm run build`
- 结果：通过。

### 已知状态

- Vite 构建提示 Three.js 相关 chunk 偏大；当前 3D 组件已 lazy load，不影响运行。
- 真实坐标质量取决于模型输出 mask 与后处理结果，前端已兼容真实坐标和旧记录 fallback。
