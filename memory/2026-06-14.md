# 2026-06-14 阶段记录

## M1 + M2 完成

本阶段完成后端 Flask 脚手架、数据库模型、迁移与基础 CRUD。

### 已完成内容

- 初始化 git 仓库。
- 新增 Flask app factory：`backend/app.py`。
- 新增后端配置、扩展与统一错误处理：
  - `backend/config.py`
  - `backend/extensions.py`
  - `backend/errors.py`
- 新增健康检查接口：`GET /api/health`。
- 新增 SQLAlchemy ORM 模型：
  - `Patient`
  - `Diagnosis`
  - SQLite 兼容 `JSONField`
- 新增 Alembic 迁移：
  - `backend/database/migrations/versions/30496e383b13_create_patients_and_diagnoses.py`
- 新增 Patient CRUD：
  - `GET /api/patients`
  - `POST /api/patients`
  - `GET /api/patients/<id>`
  - `PUT /api/patients/<id>`
  - `DELETE /api/patients/<id>`
- 新增 Diagnosis CRUD：
  - `GET /api/diagnoses`
  - `POST /api/diagnoses`
  - `GET /api/diagnoses/<id>`
  - `PUT /api/diagnoses/<id>`
  - `DELETE /api/diagnoses/<id>`
- 新增后端测试：
  - `backend/tests/test_health.py`
  - `backend/tests/test_patients.py`
  - `backend/tests/test_diagnoses.py`

### 验证

- 使用环境：`A:\python1\envs\cardio-ai\python.exe`
- 已安装后端依赖：`backend/requirements.txt`
- 测试命令：`A:\python1\envs\cardio-ai\python.exe -m pytest backend\tests -q`
- 测试结果：`4 passed`
- 数据库迁移状态：`30496e383b13 (head)`
- 启动 `backend/app.py` 后访问 `/api/health` 返回 `200`，数据库连接和模型文件检测正常。

### 已知状态

- 当前尚未配置 git remote，因此本阶段只能本地提交，不能 push。
- `POST /api/diagnose` 上传推理接口属于 M3/M4，不在本阶段实现。
- ML 推理层、图片服务、统计接口将在后续里程碑继续实现。

## M3 完成

本阶段完成 ML 层：Attention UNet 定义、真实模型加载、推理管线与后处理工具。

### 已完成内容

- 新增 `backend/ml/model.py`
  - 与 Kaggle 训练脚本中的 Attention UNet 结构保持一致。
  - features 固定为 `(32, 64, 128, 256)`。
  - 支持输入尺寸恢复，输出 4 通道：HE/EX/MA/SE。
- 新增 `backend/ml/config.py`
  - 管理模型路径、阈值文件路径、输入尺寸、类别顺序、ImageNet 归一化参数。
  - 读取 `backend/trained_models/recommended_thresholds.json`。
- 新增 `backend/ml/utils.py`
  - 眼底 ROI 构建。
  - 连通域过滤。
  - 多类别 mask 后处理。
  - mask 恢复原图尺寸。
  - 病灶面积占比计算。
  - 病灶连通域数量计算。
  - severity 经验分级。
  - 轮廓叠加图生成。
- 新增 `backend/ml/inference.py`
  - `RealInference.load_model()`
  - `RealInference.preprocess()`
  - `RealInference.predict_masks()`
  - `RealInference.predict()`
- 新增 `backend/tests/test_ml.py`
  - 验证模型输出 shape。
  - 验证阈值文件读取。
  - 验证后处理、面积、数量、轮廓生成。
  - 使用真实 `attention_unet_dr.pth` 做 CPU 小图推理测试。

### 验证

- 使用环境：`A:\python1\envs\cardio-ai\python.exe`
- 单独 ML 测试：`A:\python1\envs\cardio-ai\python.exe -m pytest backend\tests\test_ml.py -q`
- 单独 ML 测试结果：`4 passed`
- 全部后端测试：`A:\python1\envs\cardio-ai\python.exe -m pytest backend\tests -q`
- 全部后端测试结果：`8 passed`
- 健康检查：`GET /api/health` 返回 `200`
- 模型兼容性：
  - state_dict missing keys: `0`
  - state_dict unexpected keys: `0`
  - 参数量：`7,851,872`

### 已知状态

- M3 只实现独立 ML 层，不直接改 API 调用链。
- `backend/services/inference_service.py` 与 `POST /api/diagnose` 属于 M4，将在下一阶段接入。
- 真实模型文件 `backend/trained_models/attention_unet_dr.pth` 继续被 `.gitignore` 排除，不推送到 GitHub。
